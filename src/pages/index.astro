---
import PageLayout from '../layouts/PageLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('writings'))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 5);
---

<PageLayout title="Clemente Lacerda">
  <section class="hero">
    <h1>Clemente Lacerda</h1>
    <p class="intro">
      Welcome. I write about things I find interesting and share what I'm working on.
      This is a place for my writings, ideas, and a live dashboard of my training on Strava.
    </p>
  </section>

  <hr />

  {posts.length > 0 && (
    <section class="recent-writings">
      <h2 class="label">Recent Writings</h2>
      <ul class="post-list">
        {posts.map((post) => {
          const date = post.data.date.toLocaleDateString('en-US', {
            year: 'numeric',
            month: 'short',
          });
          return (
            <li>
              <a href={`/writings/${post.id}`} class="post-link">
                <span class="post-title">{post.data.title}</span>
                <span class="post-date">{date}</span>
              </a>
            </li>
          );
        })}
      </ul>
      <a href="/writings" class="view-all">View all writings &rarr;</a>
    </section>
  )}
</PageLayout>

<style>
  .hero {
    padding-top: var(--space-3xl);
    padding-bottom: var(--space-xl);
  }

  .hero h1 {
    font-size: var(--text-3xl);
    margin-bottom: var(--space-lg);
  }

  .intro {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
    line-height: var(--leading-body);
    max-width: 38rem;
  }

  .recent-writings {
    padding-top: var(--space-xl);
  }

  .recent-writings h2 {
    margin-bottom: var(--space-lg);
  }

  .post-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-xl) 0;
  }

  .post-list li {
    margin-bottom: 0;
  }

  .post-link {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    padding: var(--space-sm) 0;
    gap: var(--space-md);
    color: var(--color-text);
    background-image: none;
    border-bottom: 1px solid var(--color-border);
    transition: color var(--transition-base);
  }

  .post-link:hover {
    color: var(--color-accent);
    background-size: 0% 0;
  }

  .post-list li:last-child .post-link {
    border-bottom: none;
  }

  .post-title {
    font-weight: 500;
  }

  .post-date {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    white-space: nowrap;
    flex-shrink: 0;
  }

  .view-all {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
  }

  .view-all:hover {
    color: var(--color-text);
  }
</style>
